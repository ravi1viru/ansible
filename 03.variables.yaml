- name : play with variables
  hosts: local
  connection: local
  vars_prompt:
   course : USERNAME
   Prompt : enter the USERNAME
   private : true
  tasks:
  - name : print course information
    ansible.builtin.debug:
     msg: "Hello {{USERNAME}} "